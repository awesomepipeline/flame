<div class="container">
  <div class="row">
    <div class="col m8 offset-m2">
        <div id="hosting">
          {{!-- Need to use model.events because this isn't fetching from Ember Data --}}
          {{#each model.events as |event|}}
            <div>{{hosting-summary-card card-data=event deleteEvent="delete"}}</div>
          {{/each}}
       </div>
    </div>
  </div>
</div>
{{!-- 
This is the session key: {{sessionKey}}
<br>
This is the username: {{username}} --}}

{{!-- f5f5f5 --}}