<div class="container">
  <div class="row">
    <div class="col m8 offset-m2">
        <div id="hosting">
          {{#each model.events as |event|}}
            <div>{{hosting-card-summary card-data=event deleteEvent="delete"}}</div>

          {{else}} 

          You have no created any events. Maybe you would like to create one now?

          {{/each}}
       </div>
    </div>
  </div>
</div>
{{!-- 
This is the session key: {{sessionKey}}
<br>
This is the username: {{username}} --}}

{{!-- f5f5f5 --}}